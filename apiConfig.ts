// apiConfig.ts

export const SERVER_API = 'http://localhost:3000';

export const API_ENDPOINTS = {
    REGISTER: `${SERVER_API}/api/register`,
    LOGIN: `${SERVER_API}/api/login`,
    USERS: `${SERVER_API}/users`,
    // Add other endpoints as needed
};
